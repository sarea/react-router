import React from 'react';
import NavBar from './navBar';

export default class About extends React.Component {
    render () {
        return (
        <div>
            <NavBar />
            <h1>About</h1>
        </div>
    );
    }
}